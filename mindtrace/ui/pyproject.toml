[project]
name = "mindtrace-ui"
version = "0.1.0"
dependencies = [
    "numpy",
    "mindtrace-core"
]

[tool.setuptools]
packages = ["mindtrace.ui"]

[tool.setuptools.package-dir]
"" = "."

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"